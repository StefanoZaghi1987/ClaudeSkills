- 2026-08-03 `~/.claude/skills/consolidate-comments/SKILL.md` — ships a best-effort, human-supervised runner; diverges from the controlled bar (knowledge graph `O1` and review-capacity calibration `O8` unmet). Caps are conservative defaults, not calibrated. Flagged in the skill's `## To be confirmed`.
- 2026-08-03 `~/.claude/skills/consolidate-specs/SKILL.md` — ships a best-effort runner; diverges from the controlled bar (graph `O1`, calibration `O8`, exclusion inventory `O14` unmet). Caps are conservative defaults; `severance` disabled until an inventory is configured. Flagged in the skill's `## To be confirmed`.
