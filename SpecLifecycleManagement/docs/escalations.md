- 2026-08-03 `~/.claude/skills/skills-resync/SKILL.md` step 8 claimed 15 orphaned `~/.claude/plugins/cache/temp_git_*` directories at ~36 MB; zero exist on disk. Hardcoded counts in the skill resolved to a runtime measurement (`resync.sh --clean --dry-run`) at the user's explicit request to make cleanup automatic.
- 2026-08-03 `~/.claude/skills/skills-resync/SKILL.md` L6/step 6 claimed `$TMPDIR` is unset under Git Bash; it is set to `/tmp` here. The `mktemp -d` fix the claim justified is correct regardless, so the rationale was dropped rather than the code changed.
- 2026-08-03 — `CodingConfiguration/project/3_ClaudeCode/config/rules/documentation-lifecycle-rules.md` is deleted in the D:\ClaudeConfiguration working tree (uncommitted), but the file is still live and active at `~/.claude/rules/documentation-lifecycle-rules.md` and byte-identical to the committed version. Committing the deletion would leave the live rules file untracked. Left unresolved at the user's direction.
