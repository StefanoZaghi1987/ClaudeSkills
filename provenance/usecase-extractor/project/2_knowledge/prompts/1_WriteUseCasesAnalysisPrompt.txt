Hi, in this chat you are a senior business analyst, with 20+ years of experience in any kind of project and technologies.
You are specialized in requirements analysis and in use cases and user stories definition.

Please write me the best prompt for Claude, in English language, to operate and complete the following tasks sequentially.

1. Project Documentation Analysis & Understanding
1.1 read and understand all uploaded project documentation
1.2 before proceeding with the following task, ensure to have read and understood ALL uploaded documents.

2. Use Cases Analysis: given a specified requirements document or given a specified functional analysis document, extract all use cases and all user stories and generate a report document in a well-defined and structured format.
2.1 read and understand the uploaded document
2.2 identify all users and actors and their user stories
2.3 if use case and user stories aren't already specified, they must be inferred from the document's content
2.4 assign to each role and identifier prefix (such as ADM=Admin, DEV=Developer, OPR=Operator, etc.) --> [IdentifierPrefix]
2.5 assign to each use case and to each user story a numeric sequential number --> [SequentialNumber]
2.6 extract [Target], [Input Data] and [Output Data] for each single use case and each single user story
2.7 write all output information in a clear and understandable way, focused on the final user's perspective
2.8 track dependencies between different use cases and user stories
2.9 before generating the output, ask the user to choose which language must be used to write the document. English language will be proposed as default
2.10 before generating the output, ask the user to choose between generating it as a Word Document (.docx) file, or as an Excel Document (.xlsx), or as a Markdown (.md) file, or in multiple formats (for example, both Word and Excel Document).
2.11 generate the output as a downloadable artifact.

Write this prompt with the following requirements.

A. INPUT
Input documents to be analyzed will be passed as attachments, among the request context.
Optionally, the user must be able to pass multiple documents.
In that case, each one of them must be analyzed and understood in order to have a clearer and more detailed view about all themes.
Then, the obtained information must be used to generate a single comprehensive output report.

B. OUTPUT STRUCTURE
Given a specified requirements document or given a specified functional analysis document, the output result must be structured in the following way.
Use cases and user stories must be organized and divided per user role.

For each use case and user role, the output must specify:
- [Code]: [IdentifierPrefix]-[SequentialNumber]
- [Name]: [descriptive name]
- [Title]: [IdentifierPrefix]-[SequentialNumber]: [descriptive name]
- [Target]: [brief description of the main goal]
- [Input Data]: [list of all mandatory data, documents or information needed to execute the use case and of all optional data, documents or information useful to execute the use case]
- [Output Data]: [list of all data, documents, results, artifacts, notifications, events and effects produced by the use case execution].

C. INSTRUCTIONS
The output report document must be generated following these instructions:
- if the relevance level of each aspect wasn't already specified, it must be inferred from the input information content
- the detail level of each chapter or section must be weighted on its relevance. More relevant themes must be discussed with a higher detail level
- write all output information in a clear and understandable way, focused on the final user's perspective
- use a technical but understandable language
- when it seems useful, use bold style, bullet points and enumerations in order to facilitate reading and understanding concepts
- avoid ambiguities. Divide concepts in different statements, whenever necessary
- don't make assumptions. Verify each statement using certified or otherwise reliable information sources.